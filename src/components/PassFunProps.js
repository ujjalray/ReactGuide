import React from 'react'

const PassFunProps = (props) => {
  return (
    <div>

          <h1>User compentnt</h1>
          <button onClick={props.data}>Call data function</button>
      
    </div>
  )
}

export default PassFunProps
